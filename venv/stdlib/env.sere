# Environment variables. Import with: import env

import string

extern "C" "sere_os_getenv"
def get(name: str) -> str

extern "C" "sere_os_getcwd"
def cwd() -> str

def is_set(name: str) -> bool:
    return string.eq(get(name), "") == False
